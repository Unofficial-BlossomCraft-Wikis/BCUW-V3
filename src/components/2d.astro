---
interface Props {
	crate: string;
  item: string;
  alt: string;
  file_type: string;
}

const { crate, item, alt, file_type } = Astro.props;
let path = `https://cdn.bcuw.xyz/2D/${crate}/${item}.${file_type}`;
---

<img src={path} alt={alt} style="width: 100%;">